export type TPostOrderItem = {
  name: string;
  image: string;
  description: string;
  price: number;
};

export type TPostReview = {
  name: string;
  image: string;
  description: string;
  price: number;
};
